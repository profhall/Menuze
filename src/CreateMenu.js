import React, {useState}from 'react';
import { menuzeRef } from './dbconnect';

const CreateMenu = () => {
    const [menu, setMenu] = useState({})

    // const CreateMenu = () => (

    // )

    return (
        <div>
            <input />
        </div>
    );
};

export default CreateMenu;